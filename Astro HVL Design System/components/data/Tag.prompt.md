Pill chip for categories, filters, or skills — optionally removable.

```jsx
<Tag brand>Propulsion</Tag>
<Tag onRemove={() => removeFilter('avionics')}>Avionics</Tag>
```

Props: `brand` (tinted), `onRemove` (adds × button). Use for team roles, project subsystems, sponsor tiers.
